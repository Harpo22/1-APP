# Optional — enables cloud accounts & cross-device sync.
# Leave unset to run Money OS in local-only mode (data stays in this browser).
#
# Get these from your Supabase project: Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
